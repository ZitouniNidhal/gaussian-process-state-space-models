#!/usr/bin/env python3
"""Benchmark base helper for GPSM benchmarks."""

from abc import ABC, abstractmethod

class BenchmarkBase(ABC):
    """Base benchmark interface."""

    @abstractmethod
    def run(self):
        raise NotImplementedError
